services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tv-jellyfin-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - adb-state:/root/.android

volumes:
  adb-state:
